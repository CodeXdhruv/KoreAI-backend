# HabitCity Backend Environment Variables

# Firebase Admin SDK credentials
# Download from Firebase Console > Project Settings > Service Accounts > Generate New Private Key
FIREBASE_CREDENTIALS_PATH=./firebase-adminsdk.json

# Database URL (SQLite for development)
DATABASE_URL=sqlite:///./habitcity.db

# For PostgreSQL production:
# DATABASE_URL=postgresql://user:password@localhost:5432/habitcity

# PPO Model paths (relative to backend root)
PPO_MODEL_PATH=models/ppo_model
VECNORM_PATH=models/vec_normalize.pkl
